/* eslint-disable @typescript-eslint/no-explicit-any */
import { http, HttpResponse } from 'msw';

// MSW handlers - currently empty since all data is managed by NgRx store
export const handlers = [];
